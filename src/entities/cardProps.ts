export type CardProps = {
  imageURL: string;
  headline: string;
  shortDescription: string;
  author?: string;
  color?: 'white' | 'grey';
  date?: string;
  shareTo: string;
  register?: string;
};
